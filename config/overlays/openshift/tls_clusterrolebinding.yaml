apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: trainer-operator-openshift-tls-profile-reader-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: trainer-operator-openshift-tls-profile-reader
subjects:
- kind: ServiceAccount
  name: controller-manager
  # Placeholder; kustomizeconfig rewrites this to the overlay namespace.
  namespace: system
